#spring.application.name=onepage
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/onepage_db"
    username: "root"
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# URL base del servicio externo (en dev = localhost)
feign:
  client:
    base-url: http://localhost:8082
